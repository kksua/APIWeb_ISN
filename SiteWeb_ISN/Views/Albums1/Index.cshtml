@model IEnumerable<APIWeb_ISN.Models.Album>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomAlbum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomAlbum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Image)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAlbum">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAlbum">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAlbum">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
