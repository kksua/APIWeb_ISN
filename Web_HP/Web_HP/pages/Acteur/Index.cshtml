@page
@model Web_HP.pages.Acteur.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Album[0].NomAlbum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Album[0].Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Album) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomAlbum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Image)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdAlbum">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdAlbum">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdAlbum">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
