@{
    Layout = "_Layout";
}
@{
    ViewData["Title"] = "Musique";
}

<!DOCTYPE html>
<html lang="en">
<head>
  <title>Music</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
</head>
<body>

<div class="container mt-3">
    <h2>Music</h2>
    
    <!-- Albums Section -->
    <h3>Albums</h3>
    <div id="albums-container"></div>
    <button class="btn btn-primary mt-2" onclick="loadMoreAlbums()">Voir plus</button>

    <!-- Songs Section -->
    <h3>Songs</h3>
    <div id="songs-container"></div>
    <button class="btn btn-primary mt-2" onclick="loadMoreSongs()">Voir plus</button>

    <!-- Singers Section -->
    <h3>Singers</h3>
    <div id="singers-container"></div>
    <button class="btn btn-primary mt-2" onclick="loadMoreSingers()">Voir plus</button>
</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
<script>
    $(document).ready(function() {
        loadMoreAlbums();
        loadMoreSongs();
        loadMoreSingers();
    });

    // Function to load more albums
    function loadMoreAlbums() {
        $.get("api/albums", function(albums) {
            // Code to append albums to albums-container
        });
    }

    // Function to load more songs
    function loadMoreSongs() {
        $.get("api/songs", function(songs) {
            // Code to append songs to songs-container
        });
    }

    // Function to load more singers
    function loadMoreSingers() {
        $.get("api/singers", function(singers) {
            // Code to append singers to singers-container
        });
    }
</script>

</body>
</html>

